Generated manifest for GATK.CalculateContamination wrapping GATK4 CalculateContamination v4.1.4.1. Module supports tumor-only and matched normal modes via optional matched.normal.input parameter. Five parameters total: 2 required (tumor.pileup.input, output) and 3 optional (matched.normal.input, tumor.segmentation.output, arguments.file). Docker image broadinstitute/gatk:4.1.4.1 is used. Wrapper script gatk_calculate_contamination_wrapper.py handles mode detection and GATK invocation. All optional tool, common, and advanced GATK arguments are excluded as GenePattern parameters per design instructions and may be passed via arguments.file. File format table;tsv;txt is supported for all file inputs. Command line uses dot-notation flags matching parameter names throughout.